spring:
  cloud:
    config:
      enabled: false
      discovery:
        enabled: false
  data:
    mongodb:
      database: test-keeper
      port: 0
      host: localhost
logging:
  level:
    org.springframework.data.mongodb.core.MongoTemplate: DEBUG